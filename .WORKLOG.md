# Autonomous Loop Worklog

Grace's creative-website exploration. This file is the single source of truth for loop state. Read it first every wake-up; update it last.

## HARD CONSTRAINTS (never violate)
1. Only edit files inside `/home/claude/workspace/grace-wj/grace-wj.github.io`. Verify `pwd` before any write/bash. No exceptions.
2. Never `cd` outside the repo. Never touch `/home/claude/.claude/*` (except reading memory).
3. Playwright may navigate to external URLs for **reference-site reconnaissance only** (read-only). Never submit forms, never hit admin/internal URLs.
4. Push branches to `origin` (grace-wj/grace-wj.github.io) — this is public. No secrets, no real personal data beyond "Grace Wei-li Juan".
5. Never force-push. Never rewrite history on `main`. Never delete branches.

## GOAL
Produce 3–4 distinct **strange but cool** creative website concepts for Grace Wei-li Juan (software engineer / polymath / visual artist). Each lives on its own branch, pushed to origin. Placeholder content; structure/aesthetic/interaction is the point. Surprise her.

## PHASES
- [x] `setup` — state files committed to main
- [ ] `recon` — playwright through 5 reference sites, notes to `.inspiration/`
- [ ] `concepts` — write 4 concepts to this file, each weird on a DIFFERENT axis
- [ ] `build` — per concept: branch, draft, self-review + iterate ONCE, push. **Hard cap: 2 iterations per branch.**
- [ ] `final` — summary + screenshots index on main

## REFERENCE SITES (recon targets)
1. https://www.narrowdesign.com
2. https://www.ozgekeles.com
3. https://reimer.tz
4. https://eli.wtf
5. https://richardmattka.com

## BUDGET
- Hard cap: 6 hours from `started_at`
- Started: (filled by first wake)
- If elapsed_hours > 5: skip to `final` phase immediately
- If all branches pushed and final summary committed: **DO NOT schedule another wake.** Loop ends.

## CONCEPTS TABLE
| name | axis-of-weirdness | branch | iterations | status | notes |
|------|-------------------|--------|------------|--------|-------|
| (filled in `concepts` phase) | | | | | |

## PER-WAKE CHECKLIST (run every wake)
1. `git status` + `git branch --show-current` — confirm repo & branch
2. `pwd` — confirm inside repo
3. Read this file's `phase` and `next_action`
4. Do ~one focused chunk of work
5. Commit if anything changed (never leave uncommitted work across wakes)
6. Update this file: phase, elapsed_hours, concepts table, next_action, blockers
7. Decide next delay (60s default; 1800s max; don't schedule if done)

## FAILURE MODES I'M GUARDING AGAINST
- **Normal-ness drift** → rubric includes "would this embarrass a design student?" as negative signal
- **Polish spiral** → hard iteration cap per branch (2). Track in table.
- **Context loss on compaction** → this file + self-contained loop prompt
- **Silent failure** → any wake ending without a commit writes a BLOCKED entry below
- **Wrong-branch edits** → checklist step 1
- **Playwright context bloat** → prefer `browser_evaluate` over full snapshots; only screenshot what's needed
- **Repo escape** → checklist step 2 + hard constraints
- **Budget overrun** → elapsed_hours check every wake; final at 5h

## BLOCKED / INCIDENTS
(none yet)

## NEXT ACTION
Begin `recon` phase: navigate to narrowdesign.com first, capture DOM structure via `browser_evaluate`, one screenshot, distill 3-5 "weird axes" in `.inspiration/01-narrowdesign.md`. Then move to next site in same wake if context allows.

## STATE
- phase: recon
- started_at: (first wake fills)
- elapsed_hours: 0
- current_branch: main
- concepts_drafted: 0
- branches_pushed: 0
